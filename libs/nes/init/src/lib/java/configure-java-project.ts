import { existsSync, readFileSync, writeFileSync } from 'fs';
import { Entry } from '../models';
import path = require('path');

export function configureJavaProject(accessToken: string, packages: Entry[]): void {
  // JAVA-TODO: Implement logic to configure a Java project
}
